rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // 誰でも読み取り可能（ゲームプレイ用）
      allow read: if true;
      
      // 書き込みはログイン済みユーザー（管理者）のみ
      allow write: if request.auth != null;
    }
  }
}
